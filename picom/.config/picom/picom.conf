# --- Backend ---
backend = "glx";
vsync = true;

# --- Fading ---
fading = true;
fade-in-step = 0.05;
fade-out-step = 0.05;

# --- Dim inactive windows ---
inactive-dim = 0.1;

# --- General ---
detect-client-opacity = true;
detect-transient = true;
use-damage = false;
